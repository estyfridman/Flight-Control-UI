import { FlightRoute } from '../Elements/FlyghtRoute/FlightRoute';
import { Station } from '../Elements/Station/Station';

export const StationStatus = () => {
    return (
        <>
            <h1> The situation picture of the airport
                represents any change in the status of the stations.</h1>
            <FlightRoute/>
        </>
    )
}